apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cert-manager

secretGenerator:
  - name: duckdns-token
    envs:
      - duckdns-token.env

generatorOptions:
  disableNameSuffixHash: true
